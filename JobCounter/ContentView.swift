import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Job Counter Competition")
            .font(.largeTitle)
            .padding()
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}

#Preview {
    ContentView()
}
